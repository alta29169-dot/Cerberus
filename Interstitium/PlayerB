-- LocalScript (Alt Account)
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")

local LocalPlayer = Players.LocalPlayer

-- ===== CONFIG =====
local TARGET_NAME = "TargetPlayerName"
local LAUNCH_VELOCITY = 100000
local SEAT_NAME = "SBTurretSeat"
local RESET_INTERVAL = 10  -- seconds between character resets

-- ===== STATE =====
local bombData = {}
local launchConn = nil
local currentBomber = nil
local resetActive = false

-- ==========================================
-- BOOST SIMULATION RADIUS
-- ==========================================
local function boostSimulationRadius()
    pcall(function()
        sethiddenproperty(LocalPlayer, "SimulationRadius", 10000)
    end)
    print("[Alt] Simulation radius boosted")
end

-- ==========================================
-- GET TARGET PLAYER
-- ==========================================
local function getTargetPlayer()
    for _, pl in ipairs(Players:GetPlayers()) do
        if pl.Name == TARGET_NAME and pl ~= LocalPlayer then
            return pl
        end
    end
    return nil
end

-- ==========================================
-- SIT IN TARGET'S BOMBER
-- ==========================================
local function sitInTargetBomber()
    local bomber = nil
    for _, obj in ipairs(workspace:GetChildren()) do
        if obj.Name == "Large Bomber" then
            local owner = obj:FindFirstChild("Owner")
            if owner and owner:IsA("StringValue") and owner.Value == TARGET_NAME then
                bomber = obj
                break
            end
        end
    end
    
    if not bomber then
        print("[Alt] No Large Bomber owned by", TARGET_NAME)
        return false
    end
    
    local seat = bomber:FindFirstChild(SEAT_NAME)
    if not seat or not seat:IsA("BasePart") then
        print("[Alt] No", SEAT_NAME, "found")
        return false
    end
    
    local char = LocalPlayer.Character
    if not char then return false end
    
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return false end
    
    hrp.CFrame = seat.CFrame + Vector3.new(0, 2, 0)
    currentBomber = bomber
    
    print("[Alt] ✅ Seated in", TARGET_NAME .. "'s Large Bomber")
    return true
end

-- ==========================================
-- RESET CHARACTER (Leave seat, respawn, re-sit)
-- ==========================================
local function resetCharacter()
    if resetActive then return end
    resetActive = true
    
    print("[Alt] 🔄 Resetting character...")
    
    -- Stop launch loop
    if launchConn then
        launchConn:Disconnect()
        launchConn = nil
    end
    
    -- Leave seat
    local char = LocalPlayer.Character
    if char then
        local humanoid = char:FindFirstChildOfClass("Humanoid")
        if humanoid then
            humanoid.Sit = false
        end
    end
    
    task.wait(0.5)
    
    -- Respawn character
    if char then
        local humanoid = char:FindFirstChildOfClass("Humanoid")
        if humanoid then
            humanoid.Health = 0  -- Kill ourselves to force respawn
        end
    end
    
    task.wait(3)  -- Wait for respawn
    
    -- Clear bomb data (fresh start)
    table.clear(bombData)
    
    -- Boost simulation radius again (new character)
    boostSimulationRadius()
    
    -- Re-sit
    task.wait(1)
    sitInTargetBomber()
    
    -- Restart launch loop
    startLaunchLoop()
    
    resetActive = false
    print("[Alt] ✅ Character reset complete")
end

-- ==========================================
-- APPLY LAUNCH TO BOMB
-- ==========================================
local function applyLaunchToBomb(bomb)
    if not bomb or not bomb.Parent then return end
    if bombData[bomb] then return end
    
    bombData[bomb] = { launched = true }
    
    task.spawn(function()
        for i = 1, 10 do
            if bomb and bomb.Parent then
                bomb.AssemblyLinearVelocity = Vector3.new(0, LAUNCH_VELOCITY, 0)
            end
            task.wait(0.05)
        end
    end)
    
    print("[Alt] 🚀 Launched bomb")
end

-- ==========================================
-- MAIN LAUNCH LOOP
-- ==========================================
local function startLaunchLoop()
    if launchConn then launchConn:Disconnect() end
    
    launchConn = RunService.Heartbeat:Connect(function(dt)
        -- Check if still seated
        local char = LocalPlayer.Character
        if char then
            local humanoid = char:FindFirstChildOfClass("Humanoid")
            if humanoid and not humanoid.SeatPart then
                sitInTargetBomber()
            end
        end
        
        -- Find and launch bombs
        for _, obj in ipairs(workspace:GetChildren()) do
            if obj.Name == "Bomb" and obj:IsA("BasePart") and not bombData[obj] then
                local myChar = LocalPlayer.Character
                local myRoot = myChar and myChar:FindFirstChild("HumanoidRootPart")
                if myRoot then
                    local dist = (obj.Position - myRoot.Position).Magnitude
                    if dist <= 300 then
                        applyLaunchToBomb(obj)
                    end
                end
            end
        end
        
        -- Clean up destroyed bombs
        for bomb, data in pairs(bombData) do
            if not bomb.Parent then
                bombData[bomb] = nil
            end
        end
    end)
end

-- ==========================================
-- STOP ALL
-- ==========================================
local function stopAll()
    if launchConn then
        launchConn:Disconnect()
        launchConn = nil
    end
    table.clear(bombData)
    currentBomber = nil
end

-- ==========================================
-- RESPAWN HANDLER
-- ==========================================
LocalPlayer.CharacterAdded:Connect(function()
    print("[Alt] Respawn detected — waiting...")
    task.wait(3)
    stopAll()
    task.wait(1)
    boostSimulationRadius()
    task.wait(0.5)
    sitInTargetBomber()
    startLaunchLoop()
end)

-- ==========================================
-- INITIAL SETUP
-- ==========================================
boostSimulationRadius()

if LocalPlayer.Character then
    task.wait(1)
    sitInTargetBomber()
    startLaunchLoop()
end

-- ==========================================
-- RESET LOOP (every 10 seconds)
-- ==========================================
task.spawn(function()
    while true do
        task.wait(RESET_INTERVAL)
        resetCharacter()
    end
end)

print("🎯 Alt turret gunner loaded (with 10s reset cycle)")
print("   Target:", TARGET_NAME)
print("   Seat:", SEAT_NAME)
print("   Reset interval:", RESET_INTERVAL, "seconds")
